sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t shvimal/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true shvimal/react-test npm test

after_success:
  - docker build -t shvimal/multi-client ./client
  - docker build -t shvimal/multi-nginx ./nginx
  - docker build -t shvimal/multi-server ./server
  - docker build -t shvimal/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push shvimal/multi-client
  - docker push shvimal/multi-nginx
  - docker push shvimal/multi-server
  - docker push shvimal/multi-worker
